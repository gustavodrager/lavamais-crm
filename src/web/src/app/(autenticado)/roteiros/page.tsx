import Link from "next/link";
import { ArrowDown, ArrowUp, MapPin, Plus } from "lucide-react";
import { adicionarParada, atualizarMotorista, criarRoteiro, editarParada, excluirRoteiro, moverParada, removerParada } from "./acoes";
import { BotaoImprimir } from "./botao-imprimir";
import { ConfirmarPublicacao } from "./confirmar-publicacao";
import { ConfirmarExclusao } from "./confirmar-exclusao";
import { CabecalhoPagina } from "@/components/cabecalho-pagina";
import { Alert, AlertDescription, AlertTitle } from "@/components/ui/alert";
import { Badge } from "@/components/ui/badge";
import { Button } from "@/components/ui/button";
import { Card, CardContent, CardHeader, CardTitle } from "@/components/ui/card";
import { Input } from "@/components/ui/input";
import { Label } from "@/components/ui/label";
import { Textarea } from "@/components/ui/textarea";
import { obterPortaCrmApi } from "@/infraestrutura/obter-porta-crm-api";

export default async function Roteiros({ searchParams }: { searchParams: Promise<{ data?: string; busca?: string; clienteId?: string; publicado?: string; erro?: string }> }) {
  const p = await searchParams;
  const data = p.data || hoje();
  const api = obterPortaCrmApi();
  const roteiro = await api.obterRoteiro(data);
  const busca = p.busca?.trim() || "";
  const clientes = busca ? await api.listarClientes(busca, 1, 8) : { itens: [], total: 0, pagina: 1, tamanhoPagina: 8 };
  const cliente = clientes.itens.find(x => x.id === p.clienteId);
  const ordem = roteiro?.paradas.map(x => x.id).join(",") || "";
  const podeReplanejar = roteiro && roteiro.situacao !== "Finalizado";

  return <>
    <div className="print:hidden"><CabecalhoPagina titulo="Roteiro diário" descricao="Organize coletas e entregas e publique a sequência para o motorista." acao={roteiro && <div className="flex gap-2"><Button asChild variant="outline"><Link href={`/meu-roteiro?data=${data}`}><MapPin />Ver como motorista</Link></Button><BotaoImprimir /></div>} /></div>
    {p.erro && <Alert variant="destructive" className="mb-4 print:hidden"><AlertTitle>Roteiro não atualizado</AlertTitle><AlertDescription>{p.erro}</AlertDescription></Alert>}
    {p.publicado && <Alert className="mb-4 print:hidden"><AlertTitle>Roteiro publicado</AlertTitle><AlertDescription>O motorista já pode acompanhar a sequência pelo celular. Paradas pendentes podem ser replanejadas durante o dia.</AlertDescription></Alert>}
    <form method="get" action="/roteiros" className="mb-4 flex max-w-sm items-end gap-2 print:hidden"><div className="flex-1"><Label htmlFor="data">Data do roteiro</Label><Input id="data" type="date" name="data" defaultValue={data} /></div><Button type="submit" variant="outline">Abrir</Button></form>
    {!roteiro ? <div className="print:hidden"><Card className="max-w-lg"><CardHeader><CardTitle>Criar roteiro</CardTitle></CardHeader><CardContent><form action={criarRoteiro} className="space-y-4"><input type="hidden" name="data" value={data} /><div><Label htmlFor="motorista">Nome do motorista</Label><Input id="motorista" name="nomeMotorista" placeholder="Ex.: Carlos" required /></div><Button><Plus />Criar roteiro de {formatarData(data)}</Button></form></CardContent></Card></div> : <>
      {roteiro.situacao === "EmPreparacao" && <div className="mb-4 flex flex-wrap gap-2 print:hidden"><form action={atualizarMotorista} className="flex gap-2"><input type="hidden" name="roteiroId" value={roteiro.id} /><input type="hidden" name="data" value={data} /><input type="hidden" name="versao" value={roteiro.versao} /><Input name="nomeMotorista" defaultValue={roteiro.nomeMotorista} aria-label="Nome do motorista" required maxLength={120} /><Button type="submit" variant="outline">Salvar motorista</Button></form><form action={excluirRoteiro}><input type="hidden" name="roteiroId" value={roteiro.id} /><input type="hidden" name="data" value={data} /><input type="hidden" name="versao" value={roteiro.versao} /><ConfirmarExclusao /></form></div>}
      <div className="print:hidden grid gap-5 xl:grid-cols-[minmax(0,1.3fr)_420px]">
        <Card><CardHeader><div className="flex items-center justify-between gap-3"><div><CardTitle>{formatarData(data)} · {roteiro.nomeMotorista}</CardTitle><p className="text-sm text-muted-foreground">{roteiro.paradas.length} paradas</p></div><Badge variant="secondary">{rotuloSituacao(roteiro.situacao)}</Badge></div></CardHeader><CardContent className="space-y-3">
          {roteiro.paradas.length === 0 ? <p className="rounded-lg border border-dashed p-8 text-center text-sm text-muted-foreground">Adicione a primeira coleta ou entrega.</p> : roteiro.paradas.map((parada, i) => {
            const pendente = parada.situacao === "Pendente";
            const vizinhaAnterior = roteiro.paradas[i - 1];
            const vizinhaSeguinte = roteiro.paradas[i + 1];
            return <div key={parada.id} className="grid grid-cols-[36px_1fr_auto] gap-3 rounded-xl border p-3"><span className="flex size-9 items-center justify-center rounded-lg bg-secondary font-semibold">{i + 1}</span><div><div className="flex flex-wrap items-center gap-2"><p className="font-medium">{parada.nomeCliente}</p><Badge variant="outline">{parada.tipo}</Badge><Badge variant="secondary">{parada.periodo}</Badge><Badge variant={parada.situacao === "NaoRealizada" ? "destructive" : "outline"}>{rotuloParada(parada.situacao)}</Badge></div><p className="mt-1 text-sm text-muted-foreground">{parada.enderecoCompleto}</p>{parada.observacao && <p className="mt-2 text-sm">{parada.observacao}</p>}{parada.motivoNaoRealizacao && <p className="mt-2 rounded-lg bg-destructive/10 p-2 text-sm text-destructive">Motivo: {parada.motivoNaoRealizacao}</p>}{podeReplanejar && pendente && <details className="mt-3"><summary className="cursor-pointer text-sm font-medium text-primary">Editar parada</summary><form action={editarParada} className="mt-3 grid gap-2 rounded-lg bg-secondary/40 p-3 sm:grid-cols-2"><input type="hidden" name="roteiroId" value={roteiro.id} /><input type="hidden" name="paradaId" value={parada.id} /><input type="hidden" name="data" value={data} /><input type="hidden" name="versao" value={roteiro.versao} /><select name="tipo" defaultValue={parada.tipo} aria-label="Tipo da parada" className="h-10 rounded-md border bg-background px-3 text-sm"><option>Entrega</option><option>Coleta</option></select><Input name="periodo" defaultValue={parada.periodo} aria-label="Período da parada" required /><Textarea name="observacao" defaultValue={parada.observacao ?? ""} maxLength={500} aria-label="Observação da parada" className="sm:col-span-2" /><Button type="submit" variant="outline" className="sm:col-span-2">Salvar alteração</Button></form></details>}</div>{podeReplanejar && <div className="flex gap-1">{pendente && <><form action={moverParada}><input type="hidden" name="roteiroId" value={roteiro.id} /><input type="hidden" name="data" value={data} /><input type="hidden" name="paradaId" value={parada.id} /><input type="hidden" name="direcao" value="cima" /><input type="hidden" name="ordem" value={ordem} /><input type="hidden" name="versao" value={roteiro.versao} /><Button size="icon" variant="outline" disabled={!vizinhaAnterior || vizinhaAnterior.situacao !== "Pendente"} aria-label="Mover para cima"><ArrowUp /></Button></form><form action={moverParada}><input type="hidden" name="roteiroId" value={roteiro.id} /><input type="hidden" name="data" value={data} /><input type="hidden" name="paradaId" value={parada.id} /><input type="hidden" name="direcao" value="baixo" /><input type="hidden" name="ordem" value={ordem} /><input type="hidden" name="versao" value={roteiro.versao} /><Button size="icon" variant="outline" disabled={!vizinhaSeguinte || vizinhaSeguinte.situacao !== "Pendente"} aria-label="Mover para baixo"><ArrowDown /></Button></form></>} {pendente && <form action={removerParada}><input type="hidden" name="roteiroId" value={roteiro.id} /><input type="hidden" name="paradaId" value={parada.id} /><input type="hidden" name="data" value={data} /><input type="hidden" name="versao" value={roteiro.versao} /><Button size="icon" variant="ghost" className="text-destructive" aria-label={`Remover parada de ${parada.nomeCliente}`}>×</Button></form>}</div>}</div>;
          })}
          {roteiro.situacao === "EmPreparacao" && roteiro.paradas.length > 0 && <ConfirmarPublicacao roteiroId={roteiro.id} data={data} versao={roteiro.versao} quantidade={roteiro.paradas.length} />}
        </CardContent></Card>
        <Card><CardHeader><CardTitle>Adicionar parada</CardTitle></CardHeader><CardContent className="space-y-4">{!podeReplanejar ? <p className="text-sm text-muted-foreground">O roteiro foi finalizado. Registre uma nova tentativa em outro roteiro.</p> : <><form className="flex gap-2"><input type="hidden" name="data" value={data} /><Input name="busca" defaultValue={busca} placeholder="Nome ou WhatsApp do cliente" /><Button variant="outline">Buscar</Button></form>{busca && !cliente && <div className="space-y-2">{clientes.itens.map(x => <Button key={x.id} asChild variant="outline" className="h-auto w-full justify-start py-3"><Link href={`/roteiros?${new URLSearchParams({ data, busca, clienteId: x.id })}`}>{x.nome}<span className="ml-auto text-xs text-muted-foreground">{x.localidade}{x.temEnderecoOperacional === false ? " · endereço incompleto" : ""}</span></Link></Button>)}</div>}{cliente && <form action={adicionarParada} className="space-y-4"><input type="hidden" name="roteiroId" value={roteiro.id} /><input type="hidden" name="data" value={data} /><input type="hidden" name="clienteId" value={cliente.id} /><input type="hidden" name="versao" value={roteiro.versao} /><div className="rounded-lg bg-secondary/50 p-3 font-medium">{cliente.nome}<p className="mt-1 text-xs text-muted-foreground">{cliente.localidade}</p>{cliente.temEnderecoOperacional === false && <p className="mt-2 text-sm text-destructive">Complete logradouro, número e cidade no cadastro antes de adicionar.</p>}</div><div><Label htmlFor="tipo">Tipo</Label><select id="tipo" name="tipo" className="flex h-10 w-full rounded-md border bg-background px-3"><option>Entrega</option><option>Coleta</option></select></div><div><Label htmlFor="periodo">Período ou horário</Label><Input id="periodo" name="periodo" placeholder="Ex.: Manhã ou 10h–12h" required /></div><div><Label htmlFor="observacao">Observação</Label><Textarea id="observacao" name="observacao" maxLength={500} /></div><Button className="w-full" disabled={cliente.temEnderecoOperacional === false}><Plus />Adicionar ao roteiro</Button></form>}</>}</CardContent></Card>
      </div>
      <div className="hidden print:block"><h1 className="text-2xl font-bold">LavaMais — Roteiro diário</h1><p className="mt-1">{formatarData(data)} · Motorista: {roteiro.nomeMotorista} · {roteiro.paradas.length} paradas · {rotuloSituacao(roteiro.situacao)}</p><ol className="mt-6 space-y-4">{roteiro.paradas.map(parada => <li key={parada.id} className="break-inside-avoid border-b pb-3"><div className="flex items-start gap-3"><strong>{parada.ordem}.</strong><div><strong>{parada.nomeCliente}</strong> — {parada.tipo} — {parada.periodo}<p>{parada.enderecoCompleto}</p><p>Telefone: {parada.whatsapp}</p>{parada.observacao && <p>Observação: {parada.observacao}</p>}{parada.motivoNaoRealizacao && <p>Motivo: {parada.motivoNaoRealizacao}</p>}</div></div></li>)}</ol></div>
    </>}
  </>;
}

const hoje = () => new Intl.DateTimeFormat("en-CA", { timeZone: "America/Sao_Paulo", year: "numeric", month: "2-digit", day: "2-digit" }).format(new Date());
const formatarData = (x: string) => new Intl.DateTimeFormat("pt-BR", { timeZone: "UTC", dateStyle: "long" }).format(new Date(`${x}T12:00:00Z`));
const rotuloSituacao = (x: string) => ({ EmPreparacao: "Em preparação", Publicado: "Publicado", EmAndamento: "Em andamento", Finalizado: "Finalizado" }[x] || x);
const rotuloParada = (x: string) => ({ Pendente: "Pendente", EmDeslocamento: "A caminho", Concluida: "Concluída", NaoRealizada: "Não realizada" }[x] || x);
